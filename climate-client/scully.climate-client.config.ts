import { ScullyConfig } from '@scullyio/scully';

export const config: ScullyConfig = {
  projectRoot: "./src",
  projectName: "climate-client",
  //pluginDir: './scully/plugins/',
  outDir: '../public/climate-static',
  routes: {
    
  }
};
